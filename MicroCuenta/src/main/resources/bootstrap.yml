spring:
  application:
    name: micro-cuenta
  cloud:
    config:
     enabled: true
     uri: http://${CONFIG_SERVER_URL:localhost}:${CONFIG_SERVER_PORT:8888}
  main:
    allow-bean-definition-overriding: true